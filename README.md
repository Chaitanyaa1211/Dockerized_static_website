# Docker Nginx Website

## Overview
This project demonstrates hosting a static website using Nginx inside a Docker container.

## Technologies
- Docker
- Nginx
- Docker Compose

## How to Run

git clone <repo>
cd docker-nginx-website
docker compose up -d

Open http://localhost:8080
